# RAG Server dependencies — locked for security
#
# Stack: LanceDB (pure Rust wheels) + FastEmbed (ONNX-based embeddings)
# This avoids the chromadb/sentence-transformers conflict with huggingface-hub.
# FastEmbed uses ONNX Runtime (pre-built) — no torch compilation required.
lancedb==0.30.2
fastembed==0.8.0
PyPDF2==3.0.1
python-docx==1.1.2
httpx==0.28.1
